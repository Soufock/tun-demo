# Go 三段式 VPN 实现方案

## 1. 目标

实现一个基于 Go + TUN + UDP 的三段式 VPN：

```text
                         Internet
                            │
                            │ UDP
                            ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│    Client    │ UDP  │    Center    │ UDP  │   Gateway    │
│              │◄────►│              │◄────►│              │
│ Mac/Windows  │      │ 公网服务器    │      │ 公司内网机器  │
│ Linux        │      │ 纯转发        │      │ TUN + 路由   │
│ TUN          │      │ 不创建 TUN    │      │              │
└──────────────┘      └──────────────┘      └──────┬───────┘
                                                   │
                                                   │
                                            公司内部网络
                                                   │
                                      ┌────────────┴────────────┐
                                      │                         │
                                192.168.0.0/24          192.168.10.0/24
```

核心要求：

1. Client 不直接连接 Gateway。
2. Gateway 不需要公网 IP。
3. Gateway 主动连接 Center。
4. Center 不进入公司内网。
5. Center 不创建 TUN。
6. Center 只负责 VPN 数据包中继和路由。
7. Gateway 负责把 VPN 流量送入公司内网。
8. Client 负责创建本地 TUN。
9. Client、Center、Gateway 三端使用统一 VPN 协议。
10. 第一阶段使用唯一 VPN IP，不实现重叠 VPN IP。
11. 第一阶段不实现复杂 NAT，先把三段链路跑通。

---

# 2. 三个组件职责

## Client

运行在用户电脑：

```text
Mac
Windows
Linux
```

职责：

```text
创建 TUN
获取 VPN IP
配置 VPN 路由
TUN -> UDP -> Center
Center -> UDP -> TUN
```

例如：

```text
Client VPN IP = 10.10.0.10
```

用户访问：

```text
192.168.0.100
```

数据：

```text
10.10.0.10 -> 192.168.0.100
```

发送给 Center。

---

# 3. Center

运行在公网服务器。

例如：

```text
103.217.197.174:19000
```

Center：

```text
不创建 TUN
不访问公司内网
不做 IP forwarding
不承担公司内网网关
```

只负责：

```text
Client <-> Gateway
```

核心数据结构：

```go
type ClientSession struct {
    ID         uint32
    VPNIP      net.IP
    Addr       *net.UDPAddr
    GatewayID  string
    LastSeen   time.Time
}

type Gateway struct {
    ID        string
    Addr      *net.UDPAddr
    Networks  []*net.IPNet
    LastSeen  time.Time
}

type Route struct {
    Network   *net.IPNet
    GatewayID string
}
```

例如：

```text
Client:

Session 10001
VPN IP 10.10.0.10

Gateway:

ID = company-a
Networks:
    192.168.0.0/24
```

Center 路由：

```text
192.168.0.0/24 -> company-a
```

---

# 4. Gateway

Gateway 部署在公司内网。

例如：

```text
Gateway:
192.168.0.50
```

公司内网：

```text
192.168.0.0/24
```

Gateway 主动连接 Center：

```text
Gateway
   │
   │ UDP
   ▼
Center
```

Gateway 不需要 Center 主动访问它。

Gateway 职责：

```text
创建 TUN
连接 Center
向 Center 注册自己的内网网段
Center -> Gateway -> TUN
TUN -> Gateway -> Center
```

---

# 5. 网络规划

第一阶段固定：

```text
VPN 网络：

10.10.0.0/24
```

Center 不占用 VPN IP。

例如：

```text
10.10.0.10 Client A
10.10.0.11 Client B
10.10.0.12 Client C
```

Gateway TUN 使用：

```text
10.10.0.2/24
```

注意：

Gateway 的 TUN IP 和 Client VPN IP 必须处于同一个 VPN 网络。

---

# 6. 协议设计

统一使用 16 字节 Header：

```text
0                   16
+-------------------+
| Header            |
+-------------------+
| Payload           |
+-------------------+
```

Header：

```go
type Header struct {
    Version   uint8
    Type      uint8
    Length    uint16
    SessionID uint32
    Sequence  uint32
}
```

实际数据：

```text
0-3     Magic
4       Version
5       Type
6-7     Length
8-11    SessionID
12-15   Sequence
16...   Payload
```

Magic：

```text
VTUN
```

Version：

```text
1
```

---

# 7. Packet Type

定义：

```go
const (
    TypeAuth       = 1
    TypeAuthOK     = 2
    TypeAuthFail   = 3

    TypeGatewayAuth     = 4
    TypeGatewayAuthOK   = 5
    TypeGatewayAuthFail = 6

    TypeIP = 7

    TypeHeartbeat = 8
    TypeHeartbeatOK = 9
)
```

第一阶段至少实现：

```text
AUTH
AUTH_OK
AUTH_FAIL
GATEWAY_AUTH
GATEWAY_AUTH_OK
GATEWAY_AUTH_FAIL
IP
```

---

# 8. Client 认证流程

Client 启动：

```text
Client
   │
   │ AUTH
   │ token
   ▼
Center
```

Center 验证 Token。

成功：

```text
Center
   │
   │ AUTH_OK
   │ SessionID = 10001
   │ VPN IP = 10.10.0.10
   ▼
Client
```

Client 收到：

```text
SessionID = 10001
VPNIP = 10.10.0.10
```

然后：

```text
创建/配置 TUN
配置 VPN 路由
开始传输 IP 数据
```

---

# 9. Gateway 注册流程

Gateway 启动后主动连接 Center：

```text
Gateway
   │
   │ GATEWAY_AUTH
   │ GatewayID
   │ Token
   │ Networks
   ▼
Center
```

例如：

```json
{
  "gateway_id": "company-a",
  "token": "123456",
  "networks": [
    "192.168.0.0/24"
  ]
}
```

Center 返回：

```text
GATEWAY_AUTH_OK
```

Center 保存：

```text
GatewayID = company-a

192.168.0.0/24
        ↓
company-a
```

---

# 10. Client -> 公司内网

例如 Client：

```text
VPN IP = 10.10.0.10
```

访问：

```text
192.168.0.100
```

完整流程：

```text
Client TUN

10.10.0.10
     │
     │
     ▼
192.168.0.100
     │
     ▼
Client UDP
     │
     ▼
Center
     │
     │ 查目标 IP
     │
     │ 192.168.0.0/24
     │
     ▼
Gateway company-a
     │
     ▼
Gateway TUN
     │
     ▼
公司内网
     │
     ▼
192.168.0.100
```

Center 只根据：

```text
目的 IP
```

找到 Gateway。

---

# 11. Gateway -> Client

公司服务器：

```text
192.168.0.100
```

回复：

```text
192.168.0.100 -> 10.10.0.10
```

Gateway TUN 收到：

```text
DST = 10.10.0.10
```

Gateway 将 IP 包发送给 Center。

Center 根据：

```text
DST VPN IP = 10.10.0.10
```

找到：

```text
SessionID = 10001
Client A
```

然后：

```text
Center
   │
   ▼
Client A
   │
   ▼
TUN
```

---

# 12. Center 的核心路由逻辑

Center 收到 IP 数据包以后：

```go
dstIP := getDestinationIP(payload)
```

如果来源是 Client：

```text
Client -> Center
```

执行：

```go
gateway := routeGateway(dstIP)

gateway.Conn.WriteToUDP(packet, gateway.Addr)
```

如果来源是 Gateway：

```text
Gateway -> Center
```

执行：

```go
session := sessionByVPNIP(dstIP)

centerConn.WriteToUDP(packet, session.Addr)
```

因此：

```text
Client -> Gateway

依据：
Destination IP -> Gateway
```

反向：

```text
Gateway -> Client

依据：
Destination VPN IP -> Client Session
```

---

# 13. Center 不创建 TUN

这一点必须严格遵守。

Center 不允许出现：

```go
water.New(...)
```

也不需要：

```bash
ip tuntap
```

也不需要：

```bash
ip addr add 10.10.0.x
```

Center 就是：

```text
UDP Server
+
Session Manager
+
Gateway Manager
+
Route Manager
```

---

# 14. Gateway 创建 TUN

Gateway Linux：

```bash
ip addr flush dev tun0
ip addr add 10.10.0.2/24 dev tun0
ip link set dev tun0 up
```

Gateway TUN：

```text
tun0
10.10.0.2/24
```

收到 Center 的数据：

```text
10.10.0.10 -> 192.168.0.100
```

写入 TUN。

Linux kernel 再负责：

```text
TUN
 ↓
eth0
 ↓
192.168.0.100
```

---

# 15. Gateway 必须配置公司网段路由

假设：

```text
公司 LAN：
192.168.0.0/24

Gateway：
192.168.0.50
```

Gateway 本身已经在这个 LAN 中。

Linux：

```bash
ip route
```

应该能够看到：

```text
192.168.0.0/24 dev eth0
```

同时 VPN：

```text
10.10.0.0/24 dev tun0
```

---

# 16. 返回流量问题

第一阶段要求：

```text
公司内网主机
    ↓
192.168.0.100
    ↓
10.10.0.10
```

公司内网需要知道：

```text
10.10.0.0/24
```

应该走 Gateway。

例如公司路由器增加：

```text
10.10.0.0/24
    via 192.168.0.50
```

这样：

```text
192.168.0.100
      ↓
公司路由器
      ↓
192.168.0.50 Gateway
      ↓
Gateway TUN
      ↓
Center
      ↓
Client
```

---

# 17. 第一阶段不要实现 NAT

第一阶段 VPN IP 必须唯一：

```text
Client A = 10.10.0.10
Client B = 10.10.0.11
Client C = 10.10.0.12
```

不要允许：

```text
Client A = 10.10.0.10
Client B = 10.10.0.10
```

因为第一版不实现：

```text
NAT
conntrack
五元组映射
```

这样可以大幅降低复杂度。

以后如果需要多个租户使用相同 VPN IP，再设计：

```text
SessionID
+
NAT
+
Conntrack
```

---

# 18. 项目目录

建议：

```text
vpn/
├── go.mod
│
├── protocol/
│   └── protocol.go
│
├── client/
│   └── main.go
│
├── center/
│   └── main.go
│
├── gateway/
│   └── main.go
│
└── README.md
```

公共协议必须放：

```text
protocol/protocol.go
```

Client / Center / Gateway 都引用它。

不要复制三份协议代码。

---

# 19. Center 数据结构

实现：

```go
type Center struct {
    conn *net.UDPConn

    clientsMu sync.RWMutex
    clients map[uint32]*ClientSession
    clientsByIP map[string]*ClientSession

    gatewaysMu sync.RWMutex
    gateways map[string]*Gateway

    routesMu sync.RWMutex
    routes []Route

    nextSessionID uint32
    nextVPNIP uint32
}
```

---

# 20. ClientSession

```go
type ClientSession struct {
    ID        uint32
    VPNIP     net.IP
    Addr      *net.UDPAddr
    GatewayID string
    LastSeen  time.Time
}
```

Client 认证以后：

```text
SessionID
VPNIP
UDP Address
```

都保存。

---

# 21. Gateway

```go
type Gateway struct {
    ID       string
    Addr     *net.UDPAddr
    Networks []*net.IPNet
    LastSeen time.Time
}
```

---

# 22. Route

```go
type Route struct {
    Network   *net.IPNet
    GatewayID string
}
```

例如：

```text
192.168.0.0/24 -> company-a
192.168.10.0/24 -> company-b
```

---

# 23. Gateway 注册后自动建立路由

Gateway：

```text
ID:
company-a

Networks:
192.168.0.0/24
192.168.10.0/24
```

Center 自动生成：

```text
192.168.0.0/24 -> company-a
192.168.10.0/24 -> company-a
```

Client 不需要知道 Gateway 的公网地址。

Client 只知道：

```text
Center = 103.217.197.174:19000
```

---

# 24. Client 不直接连接 Gateway

这是强制要求。

错误：

```text
Client
   ↓
Gateway
```

正确：

```text
Client
   ↓
Center
   ↓
Gateway
```

Gateway 也不能把自己的公网 UDP 地址告诉 Client 让 Client 绕过 Center。

---

# 25. Gateway 连接方式

Gateway 启动：

```text
UDP Dial Center
```

例如：

```go
conn, err := net.DialUDP(
    "udp",
    nil,
    centerAddr,
)
```

然后发送：

```text
GATEWAY_AUTH
```

之后这个 UDP 地址就作为 Gateway 的当前连接地址。

---

# 26. UDP 地址变化

不能假设 Gateway 的公网端口永久不变。

如果 Gateway NAT 映射发生变化：

```text
旧：
1.2.3.4:50001

新：
1.2.3.4:52133
```

Gateway 每次发送数据：

```text
Center
```

都可以更新：

```go
gateway.Addr = addr
gateway.LastSeen = time.Now()
```

这样可以适应 NAT。

---

# 27. Heartbeat

第二阶段增加：

```text
Gateway -> Center
TypeHeartbeat
```

Center：

```text
TypeHeartbeatOK
```

同时更新：

```go
LastSeen
```

Client 也可以使用 heartbeat。

如果：

```text
time.Since(LastSeen) > timeout
```

则认为连接失效。

---

# 28. Session 清理

Center 定期清理：

```text
ClientSession
Gateway
```

例如：

```text
超过 60 秒没有心跳
```

删除：

```text
Session
VPN IP
路由关联
```

VPN IP 重新进入地址池。

---

# 29. VPN IP 分配

Center：

```text
10.10.0.10
10.10.0.11
10.10.0.12
...
10.10.0.254
```

维护：

```go
allocated map[string]bool
```

Client AUTH：

```text
寻找未分配 IP
```

然后：

```text
SessionID = 10001
VPNIP = 10.10.0.10
```

---

# 30. Gateway 与 Client 的绑定

第一版可以简单设计为：

```text
Client
   ↓
Center
   ↓
Gateway
```

Client 不需要指定 Gateway。

Center 根据：

```text
目标公司内网 IP
```

自动选择 Gateway。

例如：

```text
192.168.0.100
```

匹配：

```text
192.168.0.0/24
```

得到：

```text
company-a
```

---

# 31. 最终完整数据流

### Client -> 公司服务器

```text
Mac
 │
 │ TUN
 │
 │ 10.10.0.10 -> 192.168.0.100
 ▼
Client
 │
 │ UDP
 ▼
Center
 │
 │ Route:
 │ 192.168.0.0/24
 ▼
Gateway
 │
 │ TUN
 ▼
公司内网
 │
 ▼
192.168.0.100
```

### 公司服务器 -> Client

```text
192.168.0.100
 │
 ▼
Gateway
 │
 │ TUN
 ▼
Gateway
 │
 │ UDP
 ▼
Center
 │
 │ dst=10.10.0.10
 │
 │ Session lookup
 ▼
Client
 │
 ▼
TUN
 │
 ▼
Mac
```

---

# 32. 实现顺序

严格按照下面顺序开发，不要一次把所有功能做完。

## Phase 1：协议

实现：

```text
protocol.go

Header
Pack
Unpack

AUTH
AUTH_OK
AUTH_FAIL
GATEWAY_AUTH
GATEWAY_AUTH_OK
GATEWAY_AUTH_FAIL
IP
```

并写单元测试。

---

## Phase 2：Center

实现：

```text
UDP Server
Client Session
Gateway Session
VPN IP Pool
Gateway Route
Client -> Gateway 转发
Gateway -> Client 转发
```

Center 不使用 TUN。

完成后可以用两个 UDP 测试程序验证：

```text
Client UDP
    ↓
Center
    ↓
Gateway UDP
```

---

## Phase 3：Gateway

实现：

```text
Linux TUN
Gateway AUTH
连接 Center
TUN -> Center
Center -> TUN
```

先不要做 NAT。

---

## Phase 4：Client

实现：

```text
Mac TUN
AUTH
动态获取 VPN IP
动态配置 TUN
配置公司网段路由
TUN -> Center
Center -> TUN
```

---

## Phase 5：真正打通

测试：

```text
Mac Client
10.10.0.10
       ↓
Center
       ↓
Gateway
192.168.0.50
       ↓
192.168.0.100
```

测试：

```bash
ping 192.168.0.100
```

然后：

```bash
curl http://192.168.0.100
```

---

# 33. 第一阶段验收标准

必须满足：

### Client

启动后：

```text
AUTH OK
SessionID=10001
VPNIP=10.10.0.10
```

### Gateway

启动后：

```text
GATEWAY AUTH OK
GatewayID=company-a
Networks:
  192.168.0.0/24
```

### Center

日志：

```text
client authenticated:
session=10001
vpn_ip=10.10.0.10

gateway authenticated:
id=company-a

route added:
192.168.0.0/24 -> company-a
```

### Client -> Gateway

日志：

```text
client -> gateway
session=10001
dst=192.168.0.100
gateway=company-a
```

### Gateway -> Client

日志：

```text
gateway -> client
gateway=company-a
dst=10.10.0.10
session=10001
```

最终：

```text
ping 192.168.0.100
```

成功。

---

# 34. 当前阶段明确禁止

第一版不要实现：

```text
NAT
多租户相同 VPN IP
复杂 ACL
Web 管理后台
数据库
Redis
Kafka
HTTPS API
用户系统
OIDC
```

先把：

```text
Client
   ↓
Center
   ↓
Gateway
   ↓
Company LAN
```

这一条数据链路彻底跑通。

---

# 35. 最终目标

最终整个 VPN 的核心模型应该非常简单：

```text
                    Center
              ┌─────────────────┐
              │                 │
Client A ────►│ Session 10001   │────► Gateway A
              │ 10.10.0.10      │
              │                 │
Client B ────►│ Session 10002   │────► Gateway A
              │ 10.10.0.11      │
              │                 │
Client C ────►│ Session 10003   │────► Gateway B
```
